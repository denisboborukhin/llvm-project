#ifndef LLVM_LIB_TARGET_TARCH_TARCHSUBTARGET_H
#define LLVM_LIB_TARGET_TARCH_TARCHSUBTARGET_H

#include "TArch.h"
#include "TArchFrameLowering.h"
#include "TArchISelLowering.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"

#define GET_SUBTARGETINFO_HEADER
#include "TArchGenSubtargetInfo.inc"

namespace llvm {

class TArchSubtarget : public TArchGenSubtargetInfo {
  TArchTargetLowering TLInfo;
  TArchFrameLowering FrameLowering;

public:
  TArchSubtarget(const Triple &TT, const std::string &CPU, const std::string &FS,
               const TargetMachine &TM);

  /// ParseSubtargetFeatures - Parses features string setting specified
  /// subtarget options.  Definition of function is auto generated by tblgen.
  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);

  const TArchTargetLowering *getTargetLowering() const override {
    TARCH_DUMP_CYAN
    return &TLInfo;
  }
  const TArchFrameLowering *getFrameLowering() const override {
    TARCH_DUMP_CYAN
    return &FrameLowering;
  }
};

} // end namespace llvm

#endif // LLVM_LIB_TARGET_TARCH_TARCHSUBTARGET_H